import React from 'react';

const Cart = ({set1,mahedi1}) => {
    // console.log(mahedi1);
    return (
        <div>
          <h1>{set1.length}</h1> 
          
        </div>
    );
};

export default Cart;